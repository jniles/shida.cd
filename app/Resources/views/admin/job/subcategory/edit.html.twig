{% set sidebar_menu_active = 'subcategories' %}

{% block title %}{{ vm.pageTitle }}{% endblock %}

{% extends '::base_admin.html.twig' %}

{% block body %}
    <header class="header header--min">
        <div class="container">
            <div class="g-content-container">
                <h2>Les sous categories</h2>
                <a href="{{ path('manager_job_subcategories_new') }}" class="btn btn-success btn-lg">+ ajouter</a>
            </div>
        </div>
    </header>

    <section class="section section--with-sidebar">
        <div class="container container--min">
            {{ form_start(edit_form, {'attr': {'id': 'form-job-subcategory'}}) }}
            <h4>Mettre a jour la sous category</h4>
            <div class="form-group">
                {{ form_label(edit_form.name, 'Nom') }}
                {{ form_widget(edit_form.name, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(edit_form.name) }}
            </div>

            <div class="form-group">
                {{ form_label(edit_form.category, 'Categorie') }}
                {{ form_widget(edit_form.category) }}
                {{ form_errors(edit_form.category) }}
            </div>

            {{ form_rest(edit_form) }}

            <div class="form-group">
                <button class="btn btn-primary" type="submit">Enregistrer</button>
            </div>
            {{ form_end(edit_form) }}
        </div>

        <div class="section__sidebar">
            {{ form_start(delete_form, {'attr': {'id': 'form-job-subcategory-delete'}}) }}
            {{ form_widget(delete_form) }}
            <p>attention une fois supprimer vous ne pouvez plus faire marche en arriere !</p>
            <button class="btn btn-danger btn-forms">Supprimer</button>
            {{ form_end(delete_form) }}
        </div>
    </section>
{% endblock %}
